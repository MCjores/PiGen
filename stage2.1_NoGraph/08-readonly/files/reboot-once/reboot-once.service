[Unit]
Description=Reboots system on first boot after initialization is done
After=regenerate_ssh_host_keys.service
Before=display-manager.service plymouth-quit.service

[Service]
Type=oneshot
ExecStart=/usr/lib/reboot_once.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
